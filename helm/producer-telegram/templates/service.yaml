apiVersion: v1
kind: Service
metadata:
  name: {{ include "producer-telegram.fullname" . }}
  labels:
    {{- include "producer-telegram.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "producer-telegram.selectorLabels" . | nindent 4 }}
